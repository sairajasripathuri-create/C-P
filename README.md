#include <stdio.h>

int main() {
    int X, N, M;
        scanf("%d %d %d", &X, &N, &M);

            // Total money Om can use = his own + Gymkhana fund
                if (X + M >= N)
                        printf("YES
");
                            else
                                    printf("NO
");

                                        return 0;
                                        }
